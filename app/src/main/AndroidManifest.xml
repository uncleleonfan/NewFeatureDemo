<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.leon.newfeaturedemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ToolbarActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <activity android:name=".CardViewActivity"/>

        <activity android:name=".RecyclerViewListActivity"/>

        <activity android:name=".RecyclerViewGridActivity"/>

        <activity android:name=".RecyclerViewStaggeredActivity"/>

        <activity android:name=".FloatingActionButtonActivity"/>

        <activity android:name=".CoordinatorLayoutActivity"/>

        <activity android:name=".SnackbarActivity"/>

        <activity android:name=".AppBarLayoutActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <activity android:name=".CollapsingToolbarLayoutActivity"
                  android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <activity android:name=".BottomNavigationViewActivity"/>

        <activity android:name=".BottomSheetDialogActivity"/>

        <activity android:name=".TextInputLayoutActivity"/>

        <activity android:name=".SwipeRefreshLayoutActivity"/>

        <activity android:name=".TabLayoutActivity"/>

        <activity android:name=".ElevationAndShadowsActivity"/>

        <activity android:name=".RippleActivity"/>

    </application>

</manifest>